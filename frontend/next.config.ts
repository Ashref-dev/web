import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // No rewrites / proxy configured — frontend will call backend directly via NEXT_PUBLIC_API_URL
};

export default nextConfig;
